* text=auto

# Always use Unix-style line endings for Bash scripts so they work in
# Docker on Windows.
.bashrc text eol=lf
*.sh text eol=lf

# Keymap release identities use raw SHA-256 values; keep them byte-stable.
*.keymap text eol=lf
*.uf2 binary
